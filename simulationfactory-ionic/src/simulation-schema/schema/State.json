{
    "$id": "State",
    "type": "object",
    "additionalProperties": false,
    "required": ["user_waiting", "turn_number", "response_deadline", "history", "prompt", "responses", "user_id"],
    "properties": {
        "turn_number": {
            "type": "number",
            "mimimum": "0"
        },
        "response_deadline": {
            "type": "string",
            "format": "date-time"
        },
        "prompt": {
            "type": "string"
        },
        "responses": {
            "type": "array",
            "items": {"type" : "string"}
        },
        "user_waiting": {
            "type": "boolean"
        },
        "history": {
            "type": "array",
            "items": {"$ref": "StateHistory"}
        },
        "user_id": {
            "type": "string"
        }
    }
}